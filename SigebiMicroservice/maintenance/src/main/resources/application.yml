spring:
  application:
    name: maintenance

  datasource:
    url: jdbc:postgresql://localhost:5433/maintenance_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8084